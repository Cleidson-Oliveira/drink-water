import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        padding: 16,
        backgroundColor: "blue",
        borderRadius: 4,
    },
    inactiveButton: {
        padding: 16,
        backgroundColor: "#88c",
        borderRadius: 4,
    },
    buttonTextContent: {
        color: "#ffffff",
        fontSize: 16,
        textAlign: "center"
    }
})